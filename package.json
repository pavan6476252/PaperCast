{
  "name": "papercast-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "prepare": "husky",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libatk1.0-0t64 libatk-bridge2.0-0t64 libcups2t64 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libasound2t64 libpango-1.0-0 libcairo2 libnss3 libnspr4 libx11-xcb1 libxss1 fonts-liberation libgtk-3-0t64 xdg-utils && npx puppeteer browsers install chrome",
    "schema:validate": "pnpm --filter @papercast/core run schema:validate",
    "web:dev": "pnpm --filter papercast-web run dev",
    "mcp:dev": "pnpm --filter papercast-mcp run dev",
    "mcp:start": "pnpm --filter papercast-mcp run dev",
    "mcp:build": "pnpm --filter papercast-mcp run build",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "publish-packages": "pnpm run build && pnpm publish -r --access public --no-git-checks"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^20",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.39.5",
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "prettier": "^3.0.0",
    "tsx": "^4.23.1",
    "turbo": "^2.0.0",
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,css,md,mdx}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pavan6476252/papercast"
  }
}
